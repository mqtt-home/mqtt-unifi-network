services:
  mqtt-unifi-network:
    build:
      context: .
      dockerfile: Dockerfile
    image: pharndt/mqtt-unifi-network:latest
    ports:
      - "8080:8080"
      - "6060:6060"
    volumes:
      - ../production/config:/var/lib/mqtt-unifi-network
    environment:
      TZ: Europe/Berlin
    restart: unless-stopped
